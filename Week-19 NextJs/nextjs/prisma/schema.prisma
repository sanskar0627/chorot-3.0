
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model USer{
  id  Int @default(autoincrement()) @id
  username String
  passwor String
}